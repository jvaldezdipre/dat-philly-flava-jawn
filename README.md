
  # Build page with containers

  This is a code bundle for Build page with containers. The original project is available at https://www.figma.com/design/0rTVaSLOzDPYdes5yQeoB0/Build-page-with-containers.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  